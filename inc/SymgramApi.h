#ifndef __SYMGRAMAPI_H__
#define __SYMGRAMAPI_H__

// Real api_id / api_hash live in the gitignored SymgramApiLocal.h.
// Copy inc/SymgramApiLocal.h.example if the local file is missing.
#include "SymgramApiLocal.h"

#endif
